{
  "name": "telebot",
  "version": "1.0.0",
  "description": "Telegram Bot Starter for Cloudflare Workers using Telegraf",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.69.0"
  },
  "dependencies": {
    "telegraf": "^4.16.3"
  }
}
